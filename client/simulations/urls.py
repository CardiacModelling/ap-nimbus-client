from django.conf.urls import re_path

from . import views


urlpatterns = [
#    re_path(
#        r'^models/$',
#        views.CellmlModelListView.as_view(),
#        name='model_list',
#    ),

]
app_name = 'simulations'
